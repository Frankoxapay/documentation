# Documentation output

Production Mintlify MDX pages belong here unless the project uses a repository-root content layout. The approved `content-map.yaml` remains the source of truth for page paths and navigation ownership.
